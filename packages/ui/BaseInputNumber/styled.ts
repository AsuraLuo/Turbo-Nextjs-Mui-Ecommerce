import styled from '@emotion/styled'

export const StyleInputNumber = styled.div`
  display: grid;
  margin: 0.75rem 0;
  grid-auto-flow: column;
  align-items: center;

  .MuiFormControl-root {
    margin-bottom: 0;
  }
`
